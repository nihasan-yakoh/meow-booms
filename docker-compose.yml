services:
  meow-game:
    build: .
    container_name: meow-game
    restart: always
    ports:
      - "9999:8080"
    environment:
      - SERVER_PORT=8080
      - SERVER_SERVLET_CONTEXT_PATH=/play
      # ถ้าอนาคตจะใช้ Redis ก็มาเพิ่มตรงนี้ได้ง่ายๆ